const getRegion=()=>{let region=document.querySelector(".notification-region");if(region)return region;region=document.createElement("div");region.className="notification-region";region.setAttribute("aria-label","Notifications");document.body.append(region);return region};export default function notification(element){const region=getRegion();const closeButton=element.querySelector(".notification-close");const duration=Math.max(0,Number.parseInt(element.dataset.duration||"0",10)||0);let timeoutId=null;let remaining=duration;let startedAt=0;const close=()=>{if(element.classList.contains("is-closing"))return;window.clearTimeout(timeoutId);element.classList.add("is-closing");element.addEventListener("animationend",()=>element.remove(),{once:true});window.setTimeout(()=>element.remove(),250)};const startTimer=()=>{if(remaining<=0)return;startedAt=Date.now();timeoutId=window.setTimeout(close,remaining)};const pauseTimer=()=>{if(!timeoutId)return;window.clearTimeout(timeoutId);timeoutId=null;remaining=Math.max(0,remaining-(Date.now()-startedAt))};if(element.parentElement!==region){region.append(element)}closeButton===null||closeButton===void 0||closeButton.addEventListener("click",close);element.addEventListener("pointerenter",pauseTimer);element.addEventListener("pointerleave",startTimer);element.addEventListener("focusin",pauseTimer);element.addEventListener("focusout",startTimer);startTimer()}